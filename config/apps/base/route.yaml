apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: lifecycle-quest
spec:
  to:
    kind: Service
    name: lifecycle-quest
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
